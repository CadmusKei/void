

export default function Profile({profileImage, className}) {
  return (

    <div className={`flex items-center justify-center aspect-square rounded-full bg-[#484848] ${className} `} >
            {/* <img src={profileImage} className="w-full h-full"></img> */}
    </div>
      
  );
}